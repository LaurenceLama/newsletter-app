import type { lists } from "@mailchimp/mailchimp_marketing";

// This is a type alias for the MembersSuccessResponse type from the Mailchimp SDK. It is better to use type directly from the SDK instead of defining a new type because it ensures that the type is always up-to-date with the SDK.
export type MembersSuccessResponse = lists.MembersSuccessResponse;
